export { WeatherCardWidget } from './ui/WeatherCardWidget';

    // const weather = this.getAttribute('weather') || '';
    // const ceiling = this.getAttribute('ceiling') || '';
    // const visibility = Number(this.getAttribute('visibility') || 0);
    // const temperature = Number(this.getAttribute('temperature') || 0);
    // const pressure = Number(this.getAttribute('pressure') || 0);
    // const windintens = Number(this.getAttribute('windintens') || 0);
    // const windforce1 = Number(this.getAttribute('windforce1') || 0);
    // const winddir = Number(this.getAttribute('winddir') || 0);
    // const ceiling_icon_status = this.getAttribute('ceiling_icon_status') as TCeilingIconStatus || '';
    // const icon_code = this.getAttribute('icon_code') as TWeatherIconCode || null;
    // const theme = this.getAttribute('theme') as 'light' | 'dark' || 'dark';

    // <WeatherCardWidget
    // weather={weather}
    // ceiling={ceiling}
    // visibility={visibility}
    // temperature={temperature}
    // pressure={pressure}
    // windintens={windintens}
    // windforce1={windforce1}
    // winddir={winddir}
    // ceiling_icon_status={ceiling_icon_status}
    // icon_code={icon_code}
    // theme={theme}
    // />